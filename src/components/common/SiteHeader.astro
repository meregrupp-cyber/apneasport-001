---
import { navigation } from '../../data/navigation';
import { routeFor, type Locale, type RouteKey } from '../../i18n/routes';
import { t } from '../../i18n/ui';
import LanguageSwitcher from './LanguageSwitcher.astro';

interface Props {
  locale: Locale;
  routeKey: RouteKey;
  alternateHref?: string | undefined;
}

const { locale, routeKey, alternateHref } = Astro.props;
const items = navigation(locale);
const copy = t(locale);
---

<header class="site-header">
  <div class="container header-inner">
    <a
      class="brand"
      href={routeFor('home', locale)}
      aria-label={locale === 'et' ? 'EAPSL avaleht' : 'EAPSL home'}
    >
      <span class="brand-mark" aria-hidden="true">
        <img src="/brand/eapsl-logo-v3.png" alt="" width="48" height="48" />
      </span>
      <span class="brand-copy">
        <strong>EAPSL</strong>
        <small>{locale === 'et' ? 'AIDA Estonia' : 'AIDA Estonia'}</small>
      </span>
    </a>

    <nav
      class="desktop-nav"
      aria-label={locale === 'et' ? 'Põhinavigatsioon' : 'Primary navigation'}
    >
      {
        items.map((item) => (
          <a href={item.href} aria-current={item.key === routeKey ? 'page' : undefined}>
            {item.label}
          </a>
        ))
      }
    </nav>

    <div class="header-actions">
      <LanguageSwitcher {locale} {routeKey} {alternateHref} />
      <a class="contact-link" href={routeFor('contact', locale)}>{copy.contact}</a>
      <details class="mobile-menu">
        <summary>{copy.menu}</summary>
        <nav aria-label={locale === 'et' ? 'Mobiilinavigatsioon' : 'Mobile navigation'}>
          {items.map((item) => <a href={item.href}>{item.label}</a>)}
          <a href={routeFor('contact', locale)}>{copy.contact}</a>
        </nav>
      </details>
    </div>
  </div>
</header>

<style>
  /* The header sits on paper, above the hero photo rather than inside it. */
  .site-header {
    position: relative;
    z-index: 50;
    color: var(--color-ink);
    background: var(--color-paper);
    border-bottom: 1px solid var(--color-line);
  }

  .header-inner {
    display: flex;
    min-height: var(--header-height);
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
  }

  .brand,
  .header-actions,
  .desktop-nav {
    display: flex;
    align-items: center;
  }

  .brand {
    gap: 0.75rem;
    text-decoration: none;
  }

  .brand-mark {
    width: 3.32rem;
    height: 3.32rem;
    flex: 0 0 auto;
  }

  /* The mark keeps its own colours; only the lettering beside it turns dark. */
  .brand-mark img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .brand-copy {
    display: grid;
    line-height: 1.05;
  }

  .brand-copy strong {
    color: var(--color-ink);
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 750;
    letter-spacing: 0.08em;
  }

  .brand-copy small {
    margin-top: 0.25rem;
    color: var(--color-muted);
    font-size: 0.66rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .desktop-nav {
    gap: clamp(0.75rem, 2vw, 1.75rem);
  }

  .desktop-nav a {
    position: relative;
    min-height: 2.75rem;
    align-content: center;
    color: var(--color-ink);
    font-family: var(--font-display);
    font-size: 0.86rem;
    font-weight: 600;
    text-decoration: none;
  }

  .desktop-nav a::after {
    position: absolute;
    right: 0;
    bottom: 0.55rem;
    left: 0;
    height: 1px;
    content: '';
    background: var(--color-ink);
    transform: scaleX(0);
    transition: transform 160ms ease;
  }

  .desktop-nav a:hover::after,
  .desktop-nav a[aria-current='page']::after {
    transform: scaleX(1);
  }

  .header-actions {
    gap: 0.75rem;
  }

  .contact-link {
    min-height: 2.75rem;
    align-content: center;
    padding-inline: 1.15rem;
    border-radius: var(--radius-sm);
    color: var(--color-paper);
    background: var(--color-ink);
    font-family: var(--font-display);
    font-size: 0.82rem;
    font-weight: 700;
    text-decoration: none;
    transition: background-color 160ms ease;
  }

  .contact-link:hover {
    background: var(--color-ink-deep);
  }

  .mobile-menu {
    display: none;
  }

  @media (max-width: 66rem) {
    .desktop-nav,
    .contact-link {
      display: none;
    }

    .mobile-menu {
      position: relative;
      display: block;
    }

    .mobile-menu summary {
      min-width: 3.5rem;
      min-height: 2.75rem;
      align-content: center;
      color: var(--color-ink);
      cursor: pointer;
      font-family: var(--font-display);
      font-size: 0.82rem;
      font-weight: 700;
      list-style: none;
      text-align: center;
    }

    .mobile-menu summary::-webkit-details-marker {
      display: none;
    }

    .mobile-menu nav {
      position: absolute;
      top: calc(100% + 0.75rem);
      right: 0;
      display: grid;
      width: min(19rem, calc(100vw - 1.25rem));
      padding: 0.5rem;
      border: 1px solid var(--color-line);
      border-radius: var(--radius-sm);
      background: var(--color-foam);
      box-shadow: var(--shadow-soft);
    }

    .mobile-menu nav a {
      min-height: 2.75rem;
      align-content: center;
      padding-inline: 0.75rem;
      border-radius: var(--radius-sm);
      color: var(--color-ink);
      font-family: var(--font-display);
      font-size: 0.9rem;
      font-weight: 600;
      text-decoration: none;
    }

    .mobile-menu nav a:hover {
      background: var(--color-paper);
    }
  }

  @media (max-width: 30rem) {
    .brand-copy small {
      display: none;
    }
  }

  @media (prefers-reduced-motion: no-preference) {
    .brand-mark img {
      animation: brand-rotation 48s linear infinite;
    }
  }

  @keyframes brand-rotation {
    to {
      transform: rotate(360deg);
    }
  }
</style>
